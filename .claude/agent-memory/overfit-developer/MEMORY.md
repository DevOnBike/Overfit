- [A commit can land mid-mutation](reference_commit_lands_mid_mutation.md) — verify `git show HEAD:<path>` after a mutation run, not just the working tree; identical bytes still show ` M` in `git status` while `git diff` is empty.
- [Gap-change floor unit](reference_gap_change_floor_unit.md) — `MinAbsoluteGapChange` gates `|slope| x span of the RETAINED ring` (8 h at shipped profiles), and only for pods that are already outliers.
- [Grep-absence ACs vs documenting the defect](reference_grep_ac_vs_documenting_the_defect.md) — "`rg X` finds nothing in <dir>" collides with the same plan's order to write X down; narrow to `*.cs`, never reword to beat the grep.
- [Doc diagnostics on arrival](reference_doc_diagnostics_on_arrival.md) — a file moved into `Sources/Anomalies` gets its XML docs checked for the first time: CS1573 per undocumented `<param>`, CS1574 per stale `<see cref>`.
- [Navigator index is stale after a move](reference_navigator_index_stale_after_move.md) — `find_references` still reports the pre-move declaration; prove call sites with a compile. Its held DLL also makes every solution build exit 1 on MSB3021/3026/3027.
- [Decode dispatcher mutation arms](reference_decode_dispatcher_mutation_arms.md) — claim-protocol mutations HANG (188 s under `--blame-hang 3m`); a mutation reds or hangs depending on which tests are in the filter; bound NO PROGRESS, never duration.
- [Mutation harness and [Theory] names](reference_mutation_theory_test_names.md) — a Theory victim reports as `Name(param:`, so exact-match membership prints a FALSE "survived"; match by prefix.
- [Test-output language; anchors need CRLF](reference_test_output_and_anchors.md) — the `dotnet test` summary language has changed twice (Polish → English 2026-08-13); match both. `\n` anchors match 0 times in this CRLF tree.
- [Raw string literals keep the FILE's line endings](reference_raw_string_literals_keep_file_eol.md) — no normalisation by the compiler; `*.cs` unpinned in `.gitattributes` + autocrlf = CRLF on Windows, so escaped `\n` finds stop matching.
- [Zero-alloc tests flake under the full suite](reference_flaky_xgboost_alloc_test.md) — two named beyond CLAUDE.md's pair (Xgboost 1/6, ResNet DAG 1/4); an extra victim in a subsystem the mutation cannot reach is noise, but name it.
- [Lab Prometheus measurement environment](reference_lab_measurement_environment.md) — port-forward ports, the stale `Tests/bin/lab-guard.json` trap, the fault-indicator screen, and why coverage must be plotted per ReplicaSet generation first.
- [Measurement mutex and the orphan test exe](reference_measurement_mutex_orphan.md) — benchmark exit 2 with no exception means something ALIVE holds it; `AbandonedMutexException` only covers a dead holder. Never build while `testhost` lives.
- [Measurement guard mechanics](reference_measurement_guard_mechanics.md) — BDN's generated build compiles Main and needs `OVERFIT_MEASUREMENT_OWNER` (without it: 0 benchmarks, exit 0); `-list classes` fires the refusal in 225 ms without running tests.
- [Benchmark host: zero summaries is ambiguous](reference_benchmark_host_zero_summaries.md) — a filter miss and a REJECTED command line look identical; `MSBuildSDKsPath` at a missing dir is the cheap way to force an all-fail build arm.
- [Coverage runsettings scope](reference_coverage_runsettings_scope.md) — the exclusion is `LanguageModels.Runtime.*`, NOT `Runtime.*`; `DevOnBike.Overfit.Runtime` IS measurable (instrumented suite 1 m 27 s vs 24 s).
- [Coverage arm decides a concurrent branch](reference_coverage_arm_matters_for_concurrent_branches.md) — same class: 0.833 branch rate under a filtered run, 1.000 under the full suite (races reach it). Say "unasserted", not "uncovered".
- [Packed-word sign extension](reference_packed_word_sign_extension.md) — a negative sub-field smears over its NEIGHBOURS, so an underflow mutation is refused by the tag check and the predicted victim stays green.
- [Grep tool mangles `//`](reference_grep_tool_mangles_double_slash.md) — Grep content output can render `//` as `\`, so sound C# reads as corrupted; confirm with Read before reacting.
- [Python mtime vs .NET ticks](reference_python_mtime_vs_dotnet_ticks.md) — same untouched file reads `.968373Z` from Python and `.9683732Z` from .NET; assert bytes exactly, mtime with a tolerance.
- [Fixture skips gate on the FILE](reference_fixture_skip_gate_on_file.md) — `Assert.SkipWhen(!Directory.Exists(dir))` still goes RED on an existing EMPTY fixture dir; gate on the file, and point the CI arm at an empty dir, not a missing one.
- [Culture-sensitive diagnostics](reference_culture_sensitive_diagnostics.md) — CA1305 is BLIND to plain `$""` (measured: 2 hits, both StringBuilder); invariant `P0` is `25 %` with a space; use `string.Create(CultureInfo.InvariantCulture, $"…")`.
- [Decode-pool wake: rendezvous, not race](reference_decode_pool_wake_rendezvous.md) — make the outcome NECESSARY FOR TERMINATION; abort-and-return, never throw. Managed `Thread.ThreadState` reads parked. Ordering mutation stays GREEN.
- [Decode-pool per-thread CPU attribution](reference_decode_pool_thread_cpu_attribution.md) — identify workers by running on them (`ForDecode` body + `GetCurrentThreadId`), not by name; scoping does NOT close the starved-spinner false GREEN, `ThreadState` does.
- [.editorconfig misses source-generated trees](reference_editorconfig_misses_generated_trees.md) — no section reaches a generated tree (identity, not path); `warning` vs `error` vs absent is the diagnosis; fix with a global AnalyzerConfig. `using System.Linq;` alone is an INERT RS0030 probe.
- [Check the HEAD blob before normalising EOL](reference_eol_check_head_blob.md) — `Anomalies.csproj` is LF despite `autocrlf=true`; a blanket CRLF pass rewrites the file and `git diff` hides it. `.cs` in HEAD is LF, so a NEW file written LF needs no pass and its anchors use `\n`.
- [Mutating an armed analyzer](reference_mutating_an_armed_analyzer.md) — a rule at `error` in a product dir fails the BUILD before any test; `-p:RunAnalyzersDuringBuild=false` keeps the in-process analyzer tests as the oracle.
- [Self-consistency tests are mutation-blind](reference_self_consistency_mutation_blind.md) — a mutation that moves subject and oracle through the SAME local leaves an "X reproduces Y" test green; desynchronise them and run both arms.
- [`:F0` on a real is NOT culture-invariant](reference_f0_on_reals_is_invariant.md) — `+Infinity`→`∞`, `NaN`→`epäluku`/`ليس رقم`; a probe missing NaN/±Inf, or using only `pl-PL`, returns the WRONG verdict. Rule defect ⇔ invariant for EVERY value the type can hold.
- [Default-valued mechanism = blind mutation arm](reference_default_valued_mechanism_blind_arm.md) — a broken default is indistinguishable from a correct one while both equal `default(T)`; the `OVERFIT_TILED_PREFILL=1` arm is what discriminates, and it doubles as the capability probe.
- [A gentle mutation reads as GREEN](reference_gentle_mutation_reads_as_green.md) — inert vs never-reached look identical; discriminate with a destructive probe + a dll hash. Dead band measured: 0.9 inert, 0.7 red.
- [ILGPU route facts](reference_ilgpu_route_facts.md) — driver-only (`nvcuda`), toolkit names live in ILGPU.Algorithms; a `b.Cuda()` context still contains a CPU device; Synchronize moved a headline 0.13x -> 1036.58x.
- [A canary needs symmetric warm-up](feedback_warmup_symmetry_canary.md) — cold-vs-warm made an idle machine read -93.5 %; read EVERY line of a timed run, not just the arm you wanted.
- [string.Create + interpolation chains](reference_string_create_interpolation_chain.md) — CS1620 "must be passed with the 'ref' keyword" really means one operand of the `+` chain lacks `$`.
- [Provenance record for an unexplained result](reference_provenance_record.md) — `Scripts/provenance.py`; its mtime check sees only source-newer-than-dll, `--compare` is the other arm.
- [Core-count lever for conv scaling](reference_core_count_lever_for_conv.md) — use `PROF_AFFINITY`+`DOTNET_PROCESSOR_COUNT`; `OVERFIT_PARALLEL_WORKERS` leaves `ResolveMBlocks` sized for 32 and misses the bar by 17%.
- [A harness carries its own library copy](reference_harness_carries_own_library_copy.md) — ProfHarness/OccupancyHarness bin holds a stale `DevOnBike.Overfit.dll`; building Main or Tests does NOT refresh it, and both A/B arms then run the old default.
- [mutate.py verdict(): the false ESCAPED, FIXED](reference_mutate_verdict_false_escaped.md) — this runner prints `Failed <FQN>`, not `[FAIL]`; 11 red tests read as "none noticed". Third outcome added; `mutation()` still LF-converts a CRLF file.
- [Price duplicated work at ONE core](reference_price_duplication_at_one_core.md) — force the split lever, run 1 core: measured 7% per extra gather against a cost model saying 22%.
