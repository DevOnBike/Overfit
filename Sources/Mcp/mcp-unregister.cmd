@echo off
REM Removes the Overfit MCP server from Claude Code (run from the same directory the
REM registration was made in - scope is the current directory's Claude project).
call claude mcp remove overfit
