# Memory index

- [Measured baselines — pointer](reference_measured_baselines.md) — canonical numbers now live in `docs/measured-baselines.md`; don't duplicate, cite + re-verify per row.
- [Assembly graph](project_assembly_graph.md) — verified 2026-08-06 project-reference graph for all of `Sources/`, `Tests/AotSmokeTest`, `Tests/Tests.csproj`, `Demo/*`.
- [Where design reasoning already lives](reference_design_reasoning_locations.md) — index of docs/comments to link instead of restating, for recurring boundary questions.
- [Guard telemetry Meter — architecture finding](project_guard_telemetry_meter.md) — 2026-08-06: multi-scope `Meter` ownership resolved (composition-root-owned observable instruments over `GuardTelemetry.Catalog`, not per-instance push-recording); no ADR needed.
- [aiops-architecture.md written](project_aiops_architecture_doc.md) — 2026-08-09, how-it-works doc for the anomaly guard; found the RunCycle/LiveMonitoringPipeline execution-path split and an IncidentTracker doc/code contradiction (overlap veto removed).
- [Metric-window-source seam — signed APPROVED](project_metric_window_source_seam.md) — 2026-08-08: `IMetricWindowSource` extraction + `now`-parameterisation for replay. Key catch: "call twice, same instance" is NOT a determinism test when the class is stateful (`IncidentTracker` continuation flips Opened→Ongoing) — needs two cold instances, ≥2 cycles. No TimeProvider, no ADR. Perf-audit follow-up: cite whole-cycle `AnomalyGuardScaleBenchmark` (4.174ms@4pods–701ms@200pods) over component benchmarks; unqualified "N seconds" claims need a stated scale (168× spread here).
- [AN-D1 peer novelty — SIGNED](project_an_d1_peer_novelty.md) — 2026-08-10: standing/reassertion design (not full suppression), ADR 0001 (LearnedState persistence), `RunCustomPeer` is a second in-scope caller found via `find_callers`, `BoundedSamples` rejected mid-implementation for a ring buffer.
- [Amendment sweep](feedback_amendment_sweep.md) — after amending a signed doc, grep the WHOLE file for the rejected term, not just the section edited; caught missing on AN-D1 by team lead, not by me.
- [AN-D9 coverage signal — SIGNED](project_an_d9_coverage_signal.md) — 2026-08-10: `up{namespace,job,pod}` not a repurposed channel (container-scraped ones miss RS-6 entirely, verified live); `RunCustomPeer` has no `IsWarmingUp` gate so a coverage scalar needs CreatedAt-bounding in the caller, not the shared path; `FloorCalibrator` has no custom-channel opt-out from auto-fit (same trap as ContainerRestarts).
- [XC-26 blocking rent — SIGNED (b)](project_xc26_blocking_rent.md) — 2026-08-12: `TryRentAsync` deferred (async rent buys back only the QUEUE — the thread is held for the whole generation anyway); do NOT reject it on alloc/AOT grounds. OVERFIT040 is structurally blind to every first-party blocking primitive; `MeanQueueWaitMs` is computed, never exported, and excludes timed-out waits.
- [XC-22 GuardMetricsEndpoint → Anomalies — SIGNED](project_xc22_guard_metrics_endpoint.md) — 2026-08-12: "stays internal" was FALSE (Anomalies grants IVT to Tests/Benchmarks, not to `overfit`); `Hosting/` is all-public. AotSmokeTest references Main ONLY. Cli↔Anomalies differ in exactly 2 analyzer rules + 12 banned symbols + GenerateDocumentationFile. Stale race comment in `ServeAsync` — the lock closed it 6 days earlier.
- [XC-49 decode-claim seam — SIGNED](project_xc49_decode_claim_seam.md) — 2026-08-14: rejected a state-WRITING test seam on the live decode pool (fast-suite classes run in parallel and decode concurrently; `ForDecode`'s completion spin has no timeout → hang, or a stale unmanaged fn-ptr → crash). Decided: parameterise the claim protocol so the test drives a local. Found a residual publication-order race in the fix itself (`_decodeChunkCount` published before the claim word).
- [XC-50 decode claim bound — SIGNED](project_xc50_decode_claim_bound.md) — 2026-08-14: bound packed into the claim word (not a store reorder — that option is *sufficient* but untestable, and this method's ordering argument has been wrong twice). Corrects my own XC-49 §A.5 claim that the seam is fix-invariant; a hand-seeded state cannot catch a publication defect.
- [XC-52 dispatcher invariant — SIGNED](project_xc52_dispatcher_invariant.md) — 2026-08-14: decided **no progress deadline** in `ForDecode`'s completion spin — not a threshold question: all 8 call sites pass a **stack local inside `fixed`** as `context`, so any early return releases the pins while a worker may still write → hang becomes heap corruption. `FailFast` ranked second (never returns). Also: the invariant's extent was wrong (descriptor access, not method extent); `_decodePoolSize` can be 1 → `ForDecode` inline → a decode test green without dispatching (capability gate needed); `coverlet.runsettings` does NOT exclude `DevOnBike.Overfit.Runtime`.
- [XC-51 analyzer message rot — SIGNED](project_xc51_message_rot.md) — 2026-08-15: **live advice vs historical record** (`git log -S` before correcting prose that names a deleted symbol — `RentArray` DID exist 2026-05-30→06-21, so 3 dated sites are records); composed identifiers are safe unless they **assert existence without resolution** (1 of 8); 47 descriptors, all `static readonly` fields, 3 with no `DiagnosticId` const; no pooled option exists for a reference-element array; "build succeeded" is not evidence no RS2000 fired.
- [AN-F1 seasonal reference](project_an_f1_seasonal_reference.md) — 2026-08-10: fix is priority reversal (common primary, seasonal fallback), NOT arithmetic composition (both are point estimates, not residuals — summing doubles the magnitude); `RunCustomTrend` already had the correct shape, `RunTrend` just needed to match it. Existing `SeasonalExpectationTests` don't pin per-pod divergence (all-pods-move-together fixtures only).
- [XC-58 session/stack contract — SIGNED](project_xc58_session_stack_contract.md) — 2026-08-15: chose (a) shared stack + detection throw; per-stack scratch 86.4 MiB (Qwen-3B, model ctx) vs 18.9 MiB (ctx 2048) — the stack is sized to `config.ContextLength`, never the session's, so "(b) costs memory" is FALSE once right-sized (net −67.5 MiB/client on `overfit serve`). Nothing shipped decodes 2 sessions of 1 engine concurrently.
- [XC-55 + XC-56 shared state — SIGNED](project_xc55_xc56_shared_state.md) — 2026-08-15: serialising the two parity writers does NOT fix XC-56 (a fixture-free `[Fact]` reads the flag too); `[ThreadStatic]` is only admissible when the field's default is `default(T)`. XC-55 closed by measurement: the accessor was never wrong — the .bin was re-converted after the RoPE row permute (`265fd77`) and position 0 is the identity rotation.
- [XC-65 culture-invariance analyzer (OVERFIT047) — SIGNED](project_xc65_culture_analyzer.md) — 2026-08-17: `sb.Append($"...")` has the SAME Roslyn shape as `string.Create(InvariantCulture, ...)` (both `InterpolatedStringAppendFormatted`), so the obvious rule is blind to half the sites; negative `int` is NOT culture-invariant (`U+2212` under sv-SE/lt-LT/fi-FI); `warning` severity is unavailable anywhere with live sites because of `aot-guard` + the `WarningsNotAsErrors` override trap. **AMENDED same day: my §3 was WRONG** — dates are invariant under `o O s u R r`, `TimeSpan` moves only under `g`/`G`; I generalised from one measured cell to a whole row and it false-positived on correct code in the `error` directory. **AMENDMENT 2 (D8): `F0` NOT exempted** — `+Infinity`→`∞` in all 6 cultures and `NaN`→`epäluku`, neither negative; the `pl-PL`-only tie-breaker would have said "invariant"; rule-defect vs pragma = invariant for *every value the type can represent*.
- [GPU probe route (ILGPU) — SIGNED](project_gpu_probe_route.md) — 2026-08-21: ILGPU 1.5.3 chosen (nupkg verified: 5 managed DLLs, `runtimes/` EMPTY, `CPUAccelerator` = correctness with no GPU); ComputeSharp/CUDA-P-Invoke/TorchSharp rejected with reasons. **AOT is NOT a valid objection to a GPU route** — the ban binds `Sources/Main` + AotSmokeTest, and GPU is already moat-private. QLoRA shapes: 253 `FrozenQuantizedLinear` calls/step, FFN 2048<->11008 = 78.9% of forward MACs (derived, not measured); LM head is Q6_K. **The step fraction that is this op has NEVER been measured** — blocks any end-to-end claim. Do NOT cite 1.96 TFLOP/s (that is the INFERENCE prefill kernel; training analogue is ~0.57). **AMENDED same day (dotLLM precedent, `D:\dotLLM\docs\CUDA.md` = a written eval of this exact question): route HELD, but I was WRONG that ILGPU has no tensor cores — `ILGPU.Algorithms` wraps cuBLAS FP16 GEMM, so cuBLAS is the primary arm and the number transfers across routes. Their negative result is GEMV/n=1 decode; they have NO backward pass at all. The quoted 'underperforms CPU' wording is NOT in their tree — the real data is uncited in `docs/AOT.md:113`.**
- [Auto-repack sidecar — RE-SIGNED](project_auto_repack_sidecar.md) — 2026-08-25 after `XC-119`: the heap route is a **measured negative** (9.5% slower short invocation, 2.4x memory) and was reverted; **the sidecar is a MEMORY/start-up feature, not throughput** (kernel carries 98.2% of warm prefill; sidecar 793 MiB vs 849-870 for NOT repacking); **writing one CHANGES GENERATED TEXT** (48 greedy tokens differ) — the strongest consent argument, disk-independent. repack = 1.01-1.11 s; 2.91x/1.96x resolved as COLD vs WARM. Two of my own rulings corrected: "no number rescues A-sync" was too strong, and Q1 failed on SCOPING (it never bound the arm the design moves users to).
- [XC-131 + XC-123 embeddings — SIGNED](project_xc131_xc123_embeddings.md) — 2026-08-27: `Embed` pooled the PRE-final-norm state; **the fix is free** (both states written 3 lines apart, `CachedGptStack.cs:334/336`) but `_finalHidden` is only reachable via a COPYING accessor. **The shipped test was green throughout** — it asserted unit-norm/determinism/`related>unrelated`, all true of the wrong vector: a RELATIVE oracle cannot catch an ABSOLUTE-direction defect. Per-vector parity is not a predicate over SIMILARITIES (1.4e-4 vs 8.5e-3, 60x). `/v1/embeddings` is NOT affected (typed `SentenceEmbedder`). ADR 0003 + 0004.
