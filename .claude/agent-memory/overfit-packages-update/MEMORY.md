# Memory index — overfit-packages-update

- [Pinning decisions](pinning_decisions.md) — deliberate below-latest pins (Roslyn CSharp family, Build.Framework/StringTools), each with how it was verified and on what date. OTel Prometheus beta section is history only — package removed 2026-08-10.
- [Test-only vs. tooling packages](test_only_packages.md) — which pins are genuinely contained to `Tests.csproj` vs. tooling that only looks test-adjacent (SourceLink ships in every packed NuGet); xunit v3 migration status.
- **RESOLVED, don't re-check the beta investigation**: `OpenTelemetry.Exporter.Prometheus.AspNetCore` pin (and every other OTel pin) was REMOVED from `Directory.Packages.props` on 2026-08-10 (task `XC-6`). Verified 2026-08-11 by grep (zero `OpenTelemetry.*` references anywhere) AND by building `Sources/Main` directly (zero `OVERFITPRERELEASE` warnings, build succeeded). Zero prerelease pins remain as of 2026-08-11 — re-verify this fact fresh each survey since it can regress silently.
- **`TorchSharp-cpu` pin is orphaned** — centrally pinned, zero `<PackageReference>` consumers anywhere (re-verified 2026-08-10, still true). Contradicts this agent's own role doc, which lists it as backing cross-checks. Flag each survey.
- 2026-08-11 survey detail (9 outdated packages, since largely superseded) — condensed into [[pinning-decisions]]'s 2026-08-14 entry; don't re-read this line, read that file.
- **CVE-2026-62897 (.NET 10.0.11 security release, CVSS 7.0) — RUNTIME NOW PATCHED ON THIS BOX, verified 2026-08-14.** SDK 10.0.111 and runtime 10.0.11 (NETCore/AspNetCore/WindowsDesktop) are now installed alongside 10.0.110/10.0.10 (`dotnet --list-sdks`/`--list-runtimes`); `dotnet` defaults to 10.0.111 (no `global.json`). `dotnet list package --vulnerable` clean, and now 3 days past the same-day Advisory-DB lag — trustworthy. Remember: that check is about NuGet *packages*, never the shared runtime — the install (now confirmed) is what closes the CVE, not any `PackageVersion` edit. See `XC-21`. Full detail + Roslyn-pin re-verification post-patch in [[pinning-decisions]].
- **`Scripts/check_template_pins.py` (task `XC-24`, added 2026-08-12)** — offline checker, compares every `Templates/**/*.csproj` pin against CPM / `<LastPublishedVersion>` / release-train siblings; has a self-test. **Run this first** for any "does the template need a bump" question instead of comparing by hand. 2026-08-14 run: flagged `Microsoft.Extensions.AI 10.8.0` (template) behind `10.9.0` (CPM siblings) — verdict **take**, see [[pinning-decisions]] for the changelog check (additive `RoutingChatClient`/`FailoverChatClient`, one breaking item scoped to `.OpenAI` package only, template uses only `AddChatClient`/`IChatClient`).
- 2026-08-11 follow-up: `Microsoft.Extensions.AI` (plain, not `.Abstractions`) is **correctly not centrally pinned** — non-issue, not an escape. Only direct reference anywhere is `Templates/content/OverfitChat/OverfitChat.csproj:14` (inline `Version="10.8.0"`, the deliberately-outside-CPM template). Inside the solution it's transitive-only: `AgentFrameworkDemo` resolves `Microsoft.Extensions.AI/10.7.0` (confirmed via `obj/project.assets.json` — ground truth — AND `dotnet list ... --include-transitive`, both agree) purely because `Microsoft.Agents.AI/1.17.0 DEPENDS ON Microsoft.Extensions.AI 10.7.0`. CPM only requires a central `PackageVersion` for *direct* `PackageReference`s (else `NU1008`); a transitive-only package needs no entry and `CentralPackageTransitivePinningEnabled=true` doesn't change that — it pins transitives only when a central entry already exists to pin them to. `Microsoft.Extensions.AI/10.7.0`'s own floor is `Abstractions >= 10.7.0`, satisfied by the centrally-pinned 10.8.3 with room to spare, so no conflict today. Don't flag this again unless a *direct* `PackageReference` to plain `Microsoft.Extensions.AI` appears inside the solution with no matching `Directory.Packages.props` entry.
- **8 `.csproj` files outside `Overfit.sln`** (checked 2026-08-11): `Demo/{AndroidBenchApp,OverfitChatApp,SkillEvalConsole,VoiceClone,VoiceLoop}`, `Sources/AndroidBench`, `Templates/DevOnBike.Overfit.Templates.csproj`, `Templates/content/OverfitChat/OverfitChat.csproj`. Only the last carries `PackageReference`s (`DevOnBike.Overfit`/`.Extensions.AI` @10.0.29 + `Microsoft.Extensions.AI` @10.8.0, all inline versions) — it's a `dotnet new` template consumer, correctly outside CPM (same conclusion as the consolidate skill's baseline). The other 7 have zero `PackageReference` (only `ProjectReference` to `Main`), so they add no un-audited package surface today — but none of them is covered by `dotnet build Overfit.sln`'s NuGetAudit gate if that ever changes.
