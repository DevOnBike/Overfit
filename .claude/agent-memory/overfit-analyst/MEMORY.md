- [Capability map](capability_map.md) — subsystem->path index; telemetry section covers 3 existing Meter instances + AOT verification reality (verified 2026-08-06).
- [Backlog index](backlog_index.md) — aiops-backlog.md condensed; only backlog file found; detection-quality only, no telemetry-format items.
- [Roadmap snapshot](roadmap_snapshot.md) — done items + deliberately-deferred items with reasons, telemetry/observability focus.
- [Anomaly guard metric seam](project_anomaly_guard_metric_seam.md) — `IMetricSource` has 0 implementers (dead); neither it nor `IRawMetricSource` fits (no time-range arg); real seam is a new interface over `PrometheusMetricWindowSource`; replay also needs `now` parameterised out of `RunOneCycleAsync`, no TimeProvider needed. Plan: docs/specs/anomaly-guard-metric-source-seam-plan.md.
- [Anomaly guard PSI channel](project_anomaly_guard_psi_channel.md) — `CustomMetricBinding` = zero-source-change vehicle (config-only, e2e tested); `MetricIndex` member cheap too (ContainerRestarts precedent). Live lab config is untracked `Tests/bin/lab-guard.json`, NOT the two committed `k8s/anomaly-guard/*.json` (guard.lab.json is stale/superseded). Plan: docs/specs/anomaly-guard-psi-cpu-channel-plan.md.
- [Anomaly guard runtime signals](project_anomaly_guard_runtime_signals.md) — 5 ASP.NET Core/.NET metrics (active_requests, lock_contentions, GC committed, exceptions, kestrel queue/reject) don't exist on the lab yet; needs new MeterListener code in Demo/LabWorkload (nothing in repo reads a foreign Meter today). LabLoadDriver is RPS-paced not concurrency-fixed — can't calibrate queue/contention signals as-is. Plan: docs/specs/anomaly-guard-runtime-signals-plan.md.
- [Adding-a-metric runbook](project_anomaly_guard_adding_metric_runbook.md) — shipped docs/aiops/aiops-adding-a-metric.md; new facts: config SignalKind default LoadIndependent, FloorCalibrator DOES persist custom-channel floors, 2026-08-08 seasonal-history-3x-noisier measurement, SignalCatalog vs PeerSignalCatalog are two separate classifiers.
- [AIOps business-case doc](project_aiops_business_case.md) — shipped docs/aiops/aiops-business-case.md (buyer-facing, no pricing/competitor content); sourced from client-readiness/client-flow/day-one-events/adding-a-metric/TASKS.md only, NOT the two strategy blueprints (unshipped candidate product + banned pricing content). Recommends shadow mode w/ seasonal history off until AN-F1/AN-D1 fixed.
- [AIOps docs map](project_aiops_docs_map.md) — rewrote docs/aiops/README.md as authority map; status=TASKS.md only; architecture.md beats detection-pipeline.md (AN-D10 stale point); security doc still at docs/security/, not yet copied to aiops-architecture-security.md; two strategy blueprints = internal pricing/competitor, never public.
- [AIOps coverage map](project_aiops_coverage_map.md) — shipped docs/aiops/aiops-coverage-map.md (per-failure-mode, not per-channel); live-cluster facts: AN-D12 ErrorRate-unbind IS applied live, all 12 lab-workload pods now have cpu:1 limit (repo manifest still says 200m — drift), ScrapeCoverage (AN-D9) built but NOT deployed live, GcPauseRatio/GcCommittedBytes/ThreadPoolQueueLength bound but unarmed.
- [XC-49 decode-pool test seam](project_xc49_decode_pool_test_seam.md) — CRITICAL: OverfitParallel's decode-pool background threads live for the whole test process by default (non-Android); a test writing `_decodeGen` directly wakes them → unmanaged function-pointer crash hazard, not just flaky. Safe seam = thin `internal ...ForTest` accessors (precedent: StackWeights.ForTest), never widen `PaddedClaim` field directly (CS0052). Plan: docs/specs/xc-49-decode-pool-claim-race-test-plan.md.
- [Machine exclusion symmetric/complete](project_machine_exclusion_symmetric.md) — plan docs/specs/machine-exclusion-symmetric-complete-plan.md; closes PB-7's 2 gaps (3 literal mutex names, build only probes never holds); central risk = Mutex thread-affinity vs MSBuild task threading, unverified, first task is a spike; InternalsVisibleTo already covers Tests+Benchmarks for a single-source constant.
