- [Baselines](baselines.md) — measured 2026-08-15: fast suite 2643/0/276; solution build "fails" only on the navigator DLL lock; **the AOT guard CAN be run here via Docker (recipe inside) — never report it CANNOT TELL again**; 5 packages from the solution + Templates packed separately by CI; local pack stamps `commit=N/A` but CI overrides it; `[LongFact]` gate costs ≥165 min; accepted-not-defects list.
