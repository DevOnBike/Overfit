# Memory index — overfit-packages-update

- [Pinning decisions](pinning_decisions.md) — the 4 deliberate below-latest pins (Roslyn CSharp, SemanticNavigator Workspaces, Build.Framework, OTel Prometheus beta), each with how it was verified and on what date.
- [Test-only vs. tooling packages](test_only_packages.md) — which pins are genuinely contained to `Tests.csproj` vs. tooling that only looks test-adjacent (SourceLink ships in every packed NuGet).
- 2026-08-10 (re-verified 2026-08-06/07 findings, still true, SDK still 10.0.110): 0 vulnerable packages (`--vulnerable` clean); `xunit` 2.9.3 still flagged **deprecated** (Legacy → `xunit.v3`); same 5 packages have real newer versions — Roslyn family 5.0.0→5.6.0 (held, SDK compiler still exactly 5.0.0), `Microsoft.Build.Framework` 18.0.2→18.8.2 (held, SDK MSBuild still 18.0.11, confirmed via NuGet flat-container index that no version exists between 18.0.2 and 18.0.11 even though overall latest grew 18.3.3→18.8.2), `OpenTelemetry.Exporter.Prometheus.AspNetCore` 1.15.3-beta.1→1.17.0-beta.1 (still 0 stable ever). Everything else already at feed latest. Re-verify via `dotnet list package --outdated` each run.
- **OpenTelemetry Prometheus beta already has a deep 2026-08-06 investigation** ("C0 resolved") in `docs/specs/guard-telemetry-meter-plan.md` — product doesn't need it, sole consumer is the non-AOT `Demo/LocalAgentAspNetDemo`, `Directory.Build.targets`'s `OVERFITPRERELEASE` guard is live and **currently firing** (empty accept-list, verified by building Main 2026-08-07). Don't redo the investigation — see [[pinning-decisions]] for the full pointer and the three live choices.
- **`TorchSharp-cpu` pin is orphaned** — centrally pinned, zero `<PackageReference>` consumers anywhere (re-verified 2026-08-10, still true). Contradicts this agent's own role doc, which lists it as backing cross-checks. Flag each survey.
- **OTel Prometheus `OVERFITPRERELEASE` guard accept-list still empty** as of 2026-08-10 (checked `Directory.Build.targets` directly — only a commented-out example entry). Guard still fires; no user decision taken yet. Re-check the accept-list each survey instead of re-running the C0 investigation.
- 2026-08-11: solution grew to **26 projects** (`Server.AspNet` now IN `Overfit.sln` — was flagged "outside the solution" by the consolidate skill's 2026-08-10 baseline; that baseline is now stale, its only `PackageReference` (`Microsoft.CodeAnalysis.BannedApiAnalyzers`) is a plain central-pin reference with no inline version). `Directory.Packages.props` now has **31** `PackageVersion` entries (counted via `grep -c`; role doc says 28 — also stale, cosmetic only). `dotnet build Sources/Main/Main.csproj -c Release` still fires `OVERFITPRERELEASE` today, build still succeeds (Kompilacja powiodła się), 0 other new warnings besides the pre-existing OVERFIT006 string-build ones in `Conv2DGemmKernels.cs`. `dotnet list Overfit.sln package --vulnerable --include-transitive` clean across all 26 projects; `--deprecated` still only `xunit 2.9.3` (Legacy → xunit.v3, Tests only). `--outdated` confirms the same 5 held-back Roslyn-family + `Microsoft.Build.Framework` pins as prior runs, nothing else. Re-queried NuGet flat-container index directly: `OpenTelemetry.Exporter.Prometheus.AspNetCore` still exactly 33 versions, 0 stable, latest still `1.17.0-beta.1` (no new version since 2026-08-06/10); `Microsoft.Build.Framework` 18.0.x window still only contains `18.0.2` (no `18.0.11`+ ever published in that minor — latest overall now 18.8.2, up from 18.3.3); `BenchmarkDotNet`/`BenchmarkDotNet.Diagnostics.Windows` confirmed latest stable is still 0.15.8 (0.16.0-preview.1 is the only newer, prerelease).
- 2026-08-11 follow-up: `Microsoft.Extensions.AI` (plain, not `.Abstractions`) is **correctly not centrally pinned** — non-issue, not an escape. Only direct reference anywhere is `Templates/content/OverfitChat/OverfitChat.csproj:14` (inline `Version="10.8.0"`, the deliberately-outside-CPM template). Inside the solution it's transitive-only: `AgentFrameworkDemo` resolves `Microsoft.Extensions.AI/10.7.0` (confirmed via `obj/project.assets.json` — ground truth — AND `dotnet list ... --include-transitive`, both agree) purely because `Microsoft.Agents.AI/1.17.0 DEPENDS ON Microsoft.Extensions.AI 10.7.0`. CPM only requires a central `PackageVersion` for *direct* `PackageReference`s (else `NU1008`); a transitive-only package needs no entry and `CentralPackageTransitivePinningEnabled=true` doesn't change that — it pins transitives only when a central entry already exists to pin them to. `Microsoft.Extensions.AI/10.7.0`'s own floor is `Abstractions >= 10.7.0`, satisfied by the centrally-pinned 10.8.3 with room to spare, so no conflict today. Don't flag this again unless a *direct* `PackageReference` to plain `Microsoft.Extensions.AI` appears inside the solution with no matching `Directory.Packages.props` entry.
- **8 `.csproj` files outside `Overfit.sln`** (checked 2026-08-11): `Demo/{AndroidBenchApp,OverfitChatApp,SkillEvalConsole,VoiceClone,VoiceLoop}`, `Sources/AndroidBench`, `Templates/DevOnBike.Overfit.Templates.csproj`, `Templates/content/OverfitChat/OverfitChat.csproj`. Only the last carries `PackageReference`s (`DevOnBike.Overfit`/`.Extensions.AI` @10.0.29 + `Microsoft.Extensions.AI` @10.8.0, all inline versions) — it's a `dotnet new` template consumer, correctly outside CPM (same conclusion as the consolidate skill's baseline). The other 7 have zero `PackageReference` (only `ProjectReference` to `Main`), so they add no un-audited package surface today — but none of them is covered by `dotnet build Overfit.sln`'s NuGetAudit gate if that ever changes.
